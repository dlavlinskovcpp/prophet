# Production-shaped secure settlement topology.
# Generic remote signer and direct oracle-attester settlement are intentionally absent.
services:
  resolver-registry:
    build: { context: ../../.., dockerfile: apps/oracle-attester/Dockerfile }
    command: uvicorn src.resolver_registry_main:app --host 0.0.0.0 --port 8200
    env_file:
      - "${PROPHET_SECRET_ROOT:-/etc/prophet/devnet}/resolver-registry.env"
    volumes:
      - ${PROPHET_RUNTIME_ROOT:-/var/lib/prophet/devnet}/resolver_store:/app/resolver_store
    restart: unless-stopped

  verifier-a:
    build: { context: ../../.., dockerfile: apps/oracle-attester/Dockerfile }
    command: uvicorn src.verifier_a_main:app --host 0.0.0.0 --port 8301
    environment:
      PROPHET_VERIFIER_RUNTIME_CONFIG: /app/runtime/verifier-a.yaml
    env_file:
      - "${PROPHET_SECRET_ROOT:-/etc/prophet/devnet}/verifier-a.env"
    volumes:
      - ${PROPHET_RUNTIME_ROOT:-/var/lib/prophet/devnet}/config/verifier-a.yaml:/app/runtime/verifier-a.yaml:ro
    restart: unless-stopped

  verifier-b:
    build: { context: ../../.., dockerfile: apps/oracle-attester/Dockerfile }
    command: uvicorn src.verifier_b_main:app --host 0.0.0.0 --port 8302
    environment:
      PROPHET_VERIFIER_RUNTIME_CONFIG: /app/runtime/verifier-b.yaml
    env_file:
      - "${PROPHET_SECRET_ROOT:-/etc/prophet/devnet}/verifier-b.env"
    volumes:
      - ${PROPHET_RUNTIME_ROOT:-/var/lib/prophet/devnet}/config/verifier-b.yaml:/app/runtime/verifier-b.yaml:ro
    restart: unless-stopped

  coordinator:
    build: { context: ../../.., dockerfile: apps/oracle-attester/Dockerfile }
    command: uvicorn src.coordinator_main:app --host 0.0.0.0 --port 8400
    environment:
      PROPHET_COORDINATOR_RUNTIME_CONFIG: /app/runtime/secure-settlement.yaml
    env_file:
      - "${PROPHET_SECRET_ROOT:-/etc/prophet/devnet}/coordinator.env"
    volumes:
      - ${PROPHET_RUNTIME_ROOT:-/var/lib/prophet/devnet}/config/secure-settlement.yaml:/app/runtime/secure-settlement.yaml:ro
      - ${PROPHET_RUNTIME_ROOT:-/var/lib/prophet/devnet}/state:/var/lib/prophet/devnet
    depends_on: [verifier-a, verifier-b]
    restart: unless-stopped

  secure-settlement:
    build: { context: ../../.., dockerfile: apps/oracle-attester/Dockerfile }
    command: uvicorn src.secure_settlement_main:app --host 0.0.0.0 --port 8500
    environment:
      PROPHET_SETTLEMENT_RUNTIME_CONFIG: /app/runtime/secure-settlement.yaml
    env_file:
      - "${PROPHET_SECRET_ROOT:-/etc/prophet/devnet}/secure-settlement.env"
    volumes:
      - ${PROPHET_RUNTIME_ROOT:-/var/lib/prophet/devnet}/config/secure-settlement.yaml:/app/runtime/secure-settlement.yaml:ro
      - ${PROPHET_RUNTIME_ROOT:-/var/lib/prophet/devnet}/state:/var/lib/prophet/devnet
      - ${PROPHET_SECRET_ROOT:-/etc/prophet/devnet}/settlement-fee-payer.json:/run/secrets/settlement-fee-payer.json:ro
    depends_on: [coordinator]
    restart: unless-stopped

  matching-keeper:
    build: { context: ../../.., dockerfile: apps/matching-keeper/Dockerfile }
    env_file:
      - "${PROPHET_SECRET_ROOT:-/etc/prophet/devnet}/matching-keeper.env"
    volumes:
      - ${PROPHET_RUNTIME_ROOT:-/var/lib/prophet/devnet}/matching-keeper:/app/apps/matching-keeper/state
      - ${PROPHET_SECRET_ROOT:-/etc/prophet/devnet}/keeper-id.json:/app/keeper-id.json:ro
    restart: unless-stopped
