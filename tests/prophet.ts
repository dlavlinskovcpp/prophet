import { assert } from "chai";

describe.skip("prophet-oracle--e2e-robust (retired legacy single-oracle suite)", () => {
  it("keeps the legacy suite retired after full surface removal", async () => {
    assert.isTrue(true);
  });
});
